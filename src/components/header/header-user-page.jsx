import React from "react";
import Header from "./header";

const HeaderUserPage = (props) => {
  return <Header className="user-page__head" {...props} />;
};

export {HeaderUserPage};
export default HeaderUserPage;
